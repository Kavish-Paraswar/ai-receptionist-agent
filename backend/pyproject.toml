[project]
name = "backend"
version = "1.0.0"
description = "AI Receptionist automation backend for QuensultingAI Dental Clinic (RetellAI + FastAPI)"
requires-python = ">=3.12"
dependencies = [
    "fastapi[standard]>=0.128.1",
    "gspread>=6.1.0",
    "google-auth>=2.35.0",
    "httpx>=0.27.0",
]
